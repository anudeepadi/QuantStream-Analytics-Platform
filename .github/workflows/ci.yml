name: CI

on:
  push:
    branches: [development]
  pull_request:
    branches: [master, development]

jobs:
  # ── Backend unit tests ──────────────────────────────────────────
  test-backend:
    name: Python Unit Tests
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"
          cache: "pip"

      - name: Install dependencies
        run: pip install -r requirements.txt pytest pytest-asyncio pytest-mock

      - name: Run unit tests
        # Skip integration/performance tests — they require live services (Kafka, PG, Redis)
        run: >
          pytest tests/ -m "unit"
          --ignore=tests/ingestion/integration
          --ignore=tests/ingestion/performance
          -v -p no:cov

  # ── Frontend lint + type-check + build ─────────────────────────
  test-frontend:
    name: Frontend Lint, Type Check & Build
    runs-on: ubuntu-latest

    defaults:
      run:
        working-directory: src/dashboard/frontend-next

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "20"
          cache: "npm"
          cache-dependency-path: src/dashboard/frontend-next/package-lock.json

      - name: Install dependencies
        run: npm ci

      - name: Lint
        run: npm run lint

      - name: Type check
        run: npm run type-check

      - name: Build
        run: npm run build:cf
        env:
          NEXT_PUBLIC_API_BASE_URL: ${{ secrets.NEXT_PUBLIC_API_BASE_URL }}

  # ── Deploy backend to Railway staging ──────────────────────────
  deploy-backend-staging:
    name: Deploy Backend → Railway Staging
    runs-on: ubuntu-latest
    needs: [test-backend, test-frontend]
    if: github.ref == 'refs/heads/development'

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Install Railway CLI
        run: npm install -g @railway/cli

      - name: Deploy to Railway staging
        run: railway up --service api --environment staging --detach
        env:
          RAILWAY_TOKEN: ${{ secrets.RAILWAY_TOKEN }}
          RAILWAY_PROJECT_ID: ${{ secrets.RAILWAY_PROJECT_ID }}

  # ── Deploy frontend to Cloudflare Pages preview ─────────────────
  deploy-frontend-preview:
    name: Deploy Frontend → Cloudflare Pages (preview)
    runs-on: ubuntu-latest
    needs: [test-backend, test-frontend]
    if: github.ref == 'refs/heads/development'

    defaults:
      run:
        working-directory: src/dashboard/frontend-next

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "20"
          cache: "npm"
          cache-dependency-path: src/dashboard/frontend-next/package-lock.json

      - name: Install dependencies
        run: npm ci

      - name: Build
        run: npm run build:cf
        env:
          NEXT_PUBLIC_API_BASE_URL: ${{ secrets.NEXT_PUBLIC_API_BASE_URL }}

      - name: Deploy to Cloudflare Pages
        uses: cloudflare/wrangler-action@v3
        with:
          apiToken: ${{ secrets.CLOUDFLARE_API_TOKEN }}
          accountId: ${{ secrets.CLOUDFLARE_ACCOUNT_ID }}
          command: pages deploy ./out --project-name=quantstream-dashboard --branch=development
          workingDirectory: src/dashboard/frontend-next
