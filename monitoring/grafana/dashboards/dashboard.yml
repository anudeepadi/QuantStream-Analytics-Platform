# Grafana dashboard provisioning configuration

apiVersion: 1

providers:
  - name: 'quantstream-dashboards'
    orgId: 1
    folder: 'QuantStream'
    type: file
    disableDeletion: false
    updateIntervalSeconds: 10
    allowUiUpdates: true
    options:
      path: /etc/grafana/provisioning/dashboards